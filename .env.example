# Painel do projeto Supabase > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Painel do projeto Supabase > Project Settings > API > service_role (secreta — nunca no cliente)
SUPABASE_SERVICE_ROLE_KEY=

# URL pública do app (usada por metadata/manifest); em local: http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
