---
description: Situação do projeto — o que está pronto, o que falta, o que fazer agora
allowed-tools: Read, Glob, Grep, Bash(git log*), Bash(git status), Bash(npm test*)
---

Me dê a situação atual do projeto, sem enfeite:

1. **Fase corrente** e o que falta para fechar o critério de saída dela (`docs/07-entrega.md`).
2. **O que existe de fato no repositório** — não o que deveria existir. Liste os módulos M1–M11 com pronto / parcial / não começado.
3. **Testes:** rodando? quantos? o que não tem cobertura?
4. **Pendências e bloqueios** de `ESTADO.md`.
5. **Suposições da Fase 0 que ainda não confirmei** — me lembre delas.
6. **Riscos que subiram de probabilidade** desde a última sessão (Anexo F).
7. **As 3 próximas coisas a fazer**, em ordem, com uma frase de justificativa cada.

Seja direto. Se algo está pela metade, diga que está pela metade.
